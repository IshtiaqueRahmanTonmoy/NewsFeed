<tabs>
    <tab
        id="@+id/tab_home"
        icon="@drawable/home"
        title="Home"
        activeColor="@color/white"
        />
    <tab
        id="@+id/tab_kids"
        icon="@drawable/mykids"
        title="Mykids"
        activeColor="@color/white" />
    <tab
        id="@+id/tab_gallery"
        icon="@drawable/gallery"
        title="Gallery"
        activeColor="@color/white"/>
    <tab
        id="@+id/tab_notification"
        icon="@drawable/notification"
        title="Notification"
        activeColor="@color/white"/>
    <tab
        id="@+id/tab_more"
        icon="@drawable/more"
        title="More"
        activeColor="@color/white"/>
</tabs>